package inky.framework.data
{
	import inky.framework.utils.ObjectProxy;


	/**
	 *
	 * An object that stores data for the model. The Model class itself is not
	 * responsible for storing any data. Instead, it creates a tree of
	 * ModelData objects and sets their properties. The ModelData class is
	 * dynamic and any property can be set on it. In addition, each of its
	 * properties is bindable.
	 *	
	 * @see inky.framework.data.Model
	 * 
	 * @langversion ActionScript 3
	 * @playerversion Flash 9.0.0
	 *
	 * @author Matthew Tretter
	 * @since  2008.06.04
	 *
	 */
	dynamic public class ModelData extends ObjectProxy
	{
	}
}
